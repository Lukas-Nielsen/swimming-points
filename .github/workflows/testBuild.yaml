name: test build

on:
    push:
        branches:
            - main
            - "dependabot/**"

permissions:
    contents: write

jobs:
    test-build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout 🛎️
              uses: actions/checkout@v6

            - name: Setup Node
              uses: actions/setup-node@v6
              with:
                  node-version: 22
                  cache: "npm"

            - name: Install and Build 🔧
              run: |
                  CI=false && npm ci
                  npm run build
